[package]
name = "resonator-control"
version = "0.1.0"
edition = "2021"
description = "Desktop control foundation for ResonantOS — macOS backend"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[example]]
name = "live_demo"

[dev-dependencies]
serial_test = "3"
